<UserControl x:Class="StockPortalApp.Views.Reports.StockValuationControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1100">
    <StackPanel>
        <!-- FILTER CARD -->
        <Border Background="White"
                BorderBrush="{StaticResource LineBrush}"
                BorderThickness="1"
                CornerRadius="14"
                Padding="20,16"
                Margin="0,0,0,20">
            <WrapPanel Orientation="Horizontal" VerticalAlignment="Center">
                <!-- ITEM -->
                <StackPanel Margin="0,0,14,10">
                    <TextBlock Text="ITEM" Style="{StaticResource FieldLabel}"/>
                    <ComboBox x:Name="ItemFilterCombo" Width="200" Height="36" Style="{StaticResource RoundedComboBox}" SelectedIndex="0" Cursor="Hand"/>
                </StackPanel>

                <!-- MIN STOCK VALUE -->
                <StackPanel Margin="0,0,14,10">
                    <TextBlock Text="MIN STOCK VALUE" Style="{StaticResource FieldLabel}"/>
                    <TextBox x:Name="MinStockValueBox" Width="160" Height="36" Tag="e.g. 0" Style="{StaticResource RoundedTextBox}"/>
                </StackPanel>

                <!-- ACTION BUTTONS -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" Margin="0,0,0,10">
                    <Button Content="Clear Filters"
                            Style="{StaticResource OutlineButton}"
                            Height="36"
                            Padding="14,0"
                            Margin="0,0,10,0"
                            Click="ClearFiltersButton_Click"/>
                    <Button Content="Apply Filters"
                            Style="{StaticResource PrimaryButton}"
                            Height="36"
                            Padding="18,0"
                            Click="ApplyFiltersButton_Click"/>
                </StackPanel>
            </WrapPanel>
        </Border>

        <!-- REPORT HEADER & SUMMARY BAR -->
        <Grid Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       Text="Stock Valuation Report"
                       FontFamily="{StaticResource HeadingFont}"
                       FontWeight="Bold"
                       FontSize="18"
                       Foreground="{StaticResource InkBrush}"
                       VerticalAlignment="Center"/>

            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                <!-- Summary Pill Badge -->
                <Border Background="#FFF5EC"
                        BorderBrush="#FCE4D6"
                        BorderThickness="1"
                        CornerRadius="20"
                        Padding="14,6"
                        Margin="0,0,12,0"
                        VerticalAlignment="Center">
                    <TextBlock x:Name="SummaryBadgeText"
                               Text="0 items · Total Stock Value: ₹0"
                               FontWeight="Bold"
                               FontSize="12.5"
                               Foreground="{StaticResource SaffronDeepBrush}"/>
                </Border>

                <!-- Export to Excel Button -->
                <Button Content="Export to Excel"
                        Style="{StaticResource OutlineButton}"
                        Height="36"
                        Padding="14,0"
                        Click="ExportExcelButton_Click"/>
            </StackPanel>
        </Grid>

        <!-- EXPLANATORY NOTE -->
        <TextBlock Text="Stock Value = current remaining quantity x the item's most recent purchase price. Items never purchased through Add Purchase show no price."
                   FontSize="11.5"
                   Foreground="{StaticResource InkSoftBrush}"
                   Margin="0,0,0,12"/>

        <!-- DATAGRID -->
        <Border Background="White"
                BorderBrush="{StaticResource LineBrush}"
                BorderThickness="1"
                CornerRadius="12"
                Padding="0">
            <Grid>
                <DataGrid x:Name="ValuationGrid"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="{StaticResource LineBrush}"
                          BorderThickness="0"
                          Background="Transparent"
                          RowHeight="42"
                          ColumnHeaderHeight="40"
                          CanUserReorderColumns="False"
                          CanUserResizeRows="False">
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#FAFAF8"/>
                            <Setter Property="Foreground" Value="{StaticResource InkSoftBrush}"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="FontSize" Value="11"/>
                            <Setter Property="Padding" Value="14,0"/>
                            <Setter Property="BorderBrush" Value="{StaticResource SaffronDeepBrush}"/>
                            <Setter Property="BorderThickness" Value="0,0,0,2"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Background" Value="White"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#FFFBF7"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#FFFBF7"/>
                                    <Setter Property="Foreground" Value="{StaticResource InkBrush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>
                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="14,0"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="DataGridCell">
                                        <Border Padding="{TemplateBinding Padding}" Background="Transparent">
                                            <ContentPresenter VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Foreground" Value="{StaticResource InkBrush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.CellStyle>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ITEM" Binding="{Binding ProductName}" Width="*"/>
                        <DataGridTextColumn Header="REMAINING QTY" Binding="{Binding RemainingQtyDisplay}" Width="180"/>
                        <DataGridTextColumn Header="LAST PURCHASE PRICE" Binding="{Binding LastPurchasePriceDisplay}" Width="200"/>
                        <DataGridTextColumn Header="STOCK VALUE" Binding="{Binding StockValueDisplay}" Width="200" ElementStyle="{StaticResource BoldCellText}"/>
                    </DataGrid.Columns>
                </DataGrid>

                <TextBlock x:Name="EmptyGridText"
                           Text="No stock valuation records found."
                           Foreground="{StaticResource InkSoftBrush}"
                           FontSize="13.5"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Margin="0,50"
                           Visibility="Collapsed"/>
            </Grid>
        </Border>
    </StackPanel>
</UserControl>
